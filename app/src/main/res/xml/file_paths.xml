<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera captures land in cache/photos/ (BeforeAfterPhotos.kt) - temporary, only until
         NewWorkViewModel reads and uploads the bytes. -->
    <cache-path name="photos" path="photos/" />
</paths>
